import React, { use } from 'react';

const Players = ({prom}) => {

    const pro = use(prom);
    console.log(pro);

 return (
    <div>
      {/* You can put your player components here later */}
    </div>
  );
};

export default Players;